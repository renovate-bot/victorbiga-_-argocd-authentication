apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: prometheus
spec:
  hosts:
    - prometheus.victor.internal
  gateways:
    - istio-system/gateway
  http:
    - name: "prometheus"
      route:
        - destination:
            host: prometheus